# Copy this file to .env and fill in your values

# Slack Bot Configuration
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_SIGNING_SECRET=your-signing-secret-here
SLACK_CHANNEL_ID=C1234567890

# Optional: Track specific users (comma-separated user IDs)
# Leave empty to track all users in channel
# Example: TRACKED_USER_IDS=U1234567890,U0987654321,U1122334455,U5566778899
TRACKED_USER_IDS=

# Server Configuration
PORT=3000

# Database Configuration
DB_PATH=time_tracking.db

# Reminder Schedule Configuration
# 
# OPTION 1: Minute-based intervals (RECOMMENDED for frequent reminders)
# REMINDER_INTERVAL_MINUTES: How often reminders are sent (in minutes)
#   Examples: 1 = every 1 minute
#            5 = every 5 minutes
#            15 = every 15 minutes
#            30 = every 30 minutes
#   Leave empty to use hour-based intervals (OPTION 2)
REMINDER_INTERVAL_MINUTES=

# OPTION 2: Hour-based intervals (use if REMINDER_INTERVAL_MINUTES is empty)
# REMINDER_MINUTE: Minute of the hour when reminders are sent (0-59)
#   Examples: 0 = every hour at :00 (9:00, 10:00, 11:00)
#            30 = every hour at :30 (9:30, 10:30, 11:30)
#            15 = every hour at :15 (9:15, 10:15, 11:15)
REMINDER_MINUTE=0

# REMINDER_INTERVAL_HOURS: How often reminders are sent (in hours)
#   Examples: 1 = every 1 hour (default)
#            2 = every 2 hours
#            4 = every 4 hours
REMINDER_INTERVAL_HOURS=1

# Image Configuration
# IMAGE_ENABLED: Enable or disable image generation in reminders
#   true = images will be generated and shown (default)
#   false = no images will be generated
IMAGE_ENABLED=true

# SERVER_URL: Your server's public URL (required for images to work)
#   Examples: http://192.168.1.100:3000
#             https://yourdomain.com
#             http://your-vps-ip:3000
#   This is used to generate image URLs that Slack can access
SERVER_URL=http://your-vps-ip:3000

# OpenAI Configuration (Optional - for humorous images in reminders)
# Get your API key from: https://platform.openai.com/api-keys
# If not set, reminders will work without images
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI Image Prompt (Optional - custom prompt for image generation)
# If set, this prompt will be used for all image generations
# If not set, a random prompt from default list will be used
# Example: OPENAI_IMAGE_PROMPT=A funny cartoon robot reminding people to check in, office setting, colorful and cheerful
OPENAI_IMAGE_PROMPT=

# Button Timeout Configuration
# BUTTON_TIMEOUT_MINUTES: How long buttons remain active after reminder is sent (in minutes)
#   After this time, all buttons will be automatically disabled
#   Examples: 3 = buttons disabled after 3 minutes (default)
#            5 = buttons disabled after 5 minutes
#            10 = buttons disabled after 10 minutes
BUTTON_TIMEOUT_MINUTES=3

